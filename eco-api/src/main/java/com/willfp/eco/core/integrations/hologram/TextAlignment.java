package com.willfp.eco.core.integrations.hologram;

/**
 * Horizontal alignment of hologram text.
 */
public enum TextAlignment {
    /** Lines are centred within the hologram. */
    CENTER,
    /** Lines are aligned to the left edge of the hologram. */
    LEFT,
    /** Lines are aligned to the right edge of the hologram. */
    RIGHT
}
